# This is a design preview, not Blaze Cannabis's live storefront.
# It carries real product data and branding, so it must not be indexed:
# a search result pointing here could be mistaken for the real shop.
User-agent: *
Disallow: /
